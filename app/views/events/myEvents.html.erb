<!--  This file is app/views/events/myEvents.html.erb -->
<h1>My Events</h1>

<%#  Part 2: Start here... %>

<div class="container-fluid">
    <div class="row">

        <div class="col-md-6">
            <h2>Hosted👑</h2>
            <% if (@host_events.empty?) %>            
                You haven't hosted any event. <%= link_to "HOST NOW🤗", new_event_path() %>
            <% else %>
            <table class="table table-striped col-md-12" id="events">
                <thead>
                    <tr>
                        <th>Title</th>
                        <th>Status</th>
                        <th>Details</th>
                        <th>Rate</th>
                    </tr>
                </thead>
                <tbody>

                        <% @host_events.each do |event| %>
                        <tr>
                        <td>
                            <%= event.title %>
                        </td>
                        <td>
                            <%= event.status %>
                        </td>
                        <td>
                            <%= link_to "Details", event_path(event), :class => 'btn btn-outline-secondary'%>
                        </td>
                        <td>
                            <!-- <%= link_to "Rate", event_path(event), :class => 'btn btn-primary'%>-->
                        </td>
                        </tr>
                        <% end %>
                </tbody>
            </table>
            <% end %>
        </div>

        <div class="col-md-6">
            <h2>Joined🙌</h2>
                <% if (@join_events.empty?) %>
                    You haven't joined any event. <%= link_to "JOIN NOW👋", events_path %>
                <% else %>
            <table class="table table-striped col-md-12" id="events">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Status</th>
                    <th>Details</th>
                    <th>Rate</th>
                </tr>
            </thead>
            <tbody>
                    <% @join_events.each do |event| %>
                    <tr>
                    <td>
                        <%= event.title %>
                    </td>
                    <td>
                        <% if event.status == 'open' %>
                        &nbsp;<span class="badge bg-success"><%= event.status %></span>
                        <% else  %>
                        &nbsp;<span class="badge bg-secondary"><%= event.status %></span>
                        <% end  %>
                    </td>
                    <td>
                        <%= link_to "Details", event_path(event), :class => 'btn btn-outline-secondary'%>
                    </td>
                    <td>
                       <!-- <%= link_to "Rate", event_path(event), :class => 'btn btn-primary'%>-->
                    </td>
                    </tr>
                    <% end %>
            </tbody>
        </table>
        <% end %>
        </div>

    </div>
</div>


