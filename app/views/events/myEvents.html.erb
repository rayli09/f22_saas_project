<!--  This file is app/views/events/myEvents.html.erb -->
<h1>My Events</h1>

<%#  Part 2: Start here... %>

<div class="container-fluid">
    <div class="row">

        <div class="col-md-6">
            <h2>Hosted👑</h2>
            <table class="table table-striped col-md-12" id="events">
                <thead>
                    <tr>
                        <th>Title</th>
                        <th>Status</th>
                        <th>Details</th>
                        <th>Rate</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (@host_events.empty?) %>
                        <tr>
                           <td><%= link_to "HOST NOW🤗", new_event_path() %></td> 
                           <td></td>
                           <td></td>
                           <td></td>
                        </tr>
                    <% else %>
                        <% @host_events.each do |event| %>
                        <tr>
                        <td>
                            <%= event.title %>
                        </td>
                        <td>
                            <%= event.status %>
                        </td>
                        <td>
                            <%= link_to "Details", event_path(event), :class => 'btn btn-primary'%>
                        </td>
                        <td>
                            <!-- <%= link_to "Rate", event_path(event), :class => 'btn btn-primary'%>-->
                        </td>
                        </tr>
                        <% end %>
                    <% end %>
                </tbody>
            </table>
        </div>

        <div class="col-md-6">
            <h2>Joined🙌</h2>
            <table class="table table-striped col-md-12" id="events">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Status</th>
                    <th>Details</th>
                    <th>Rate</th>
                </tr>
            </thead>
            <tbody>
                <% if (@join_events.empty?) %>
                    <tr>
                        <td><%= link_to "JOIN NOW👋", events_path %></td>
                        <td></td>
                        <td></td>
                        <td></td>
                    </tr>
                <% else %>
                    <% @join_events.each do |event| %>
                    <tr>
                    <td>
                        <%= event.title %>
                    </td>
                    <td>
                        <%= event.status %>
                    </td>
                    <td>
                        <%= link_to "Details", event_path(event), :class => 'btn btn-primary'%>
                    </td>
                    <td>
                       <!-- <%= link_to "Rate", event_path(event), :class => 'btn btn-primary'%>-->
                    </td>
                    </tr>
                    <% end %>
                <% end %>
            </tbody>
        </table>
        </div>

    </div>
</div>


